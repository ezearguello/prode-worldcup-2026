# Frontend (Vite) — estos dos van en el navegador, son públicos por diseño.
VITE_SUPABASE_URL=https://TUPROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key

# Seed (servidor) — NO subir a git, NO usar en el front.
# Lo pasás por línea de comando al correr el seed (ver README).
# SUPABASE_SERVICE_ROLE=tu-service-role-key
